function tenTime(number) {
  const gun = number * 10;
  return gun;
}

const result = tenTime(10);
console.log(result);

function division(number) {
  const div = number / 2;
  return div;
}

const retValue = division(50);
console.log(retValue);